class Lexeme(private val inputLexeme: String) {
  /**
   * Преобразовать лексему-объект к строковому представлению.
   * @return лексема-объект в строковом представлении.
   */
  def GetAsString(): String = inputLexeme
}
